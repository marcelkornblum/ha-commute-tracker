## Description

Describe the changes proposed in this pull request.

## Checklist

- [ ] TDD: Unit tests added or updated in `tests/` with static fixtures.
- [ ] All checks pass locally (`uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy`).
- [ ] If frontend changes are included, `npm run build` succeeds under `frontend/`.
- [ ] No intermediary transit state registered as Home Assistant entities.
